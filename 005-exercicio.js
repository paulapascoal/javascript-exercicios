let nota1 = 4.5
let nota2 = 8.5
let m = (nota1 + nota2) / 2 // por conta da regra de prioridade na execução da conta aritmética precisamos colocar a soma entre parentese para que nossa conta tenha o resultado correto. Pois nessa situação primeiro se calcula entre parentese e por último realiza a divisão.

console.log (`A média entre ${nota1} e ${nota2} é: ${m}`) 
